#include "main.h"

/**
 * print_numbers - function outputs digits 0-9
 *
 * Return: 0 (Success)
 */

void print_numbers(void)
{
	int num;

	for (num = 0; numm < 10; num++)
	{
		_putchar(num + '0');
	}
		_putchar('\n');
}
